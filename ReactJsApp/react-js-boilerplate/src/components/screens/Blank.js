import React from "react"

const Blank = props => {
    const {header}=props
    return <div>
        <h2>{header}</h2>
        <div>page under construction...</div>
    </div>
}

export default Blank
