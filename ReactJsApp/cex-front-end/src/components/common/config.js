const uriPrefix = 'http://192.168.1.3:8888/'
export const config = {
    endpoints: {
        testEndpoint: uriPrefix + 'json',
        uploaderEndpoint: uriPrefix + 'upload',
        downloadEndpoint: uriPrefix + 'download'
    },
    colors: {}
}
